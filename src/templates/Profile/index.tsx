import { LayoutDefault } from "layout/Default";
import { Playlists } from "./Playlists";

export const ProfileTemplate = () => {
  return (
    <LayoutDefault>
      <Playlists />;
    </LayoutDefault>
  );
};
